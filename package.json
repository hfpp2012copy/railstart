{
  "name": "app",
  "private": "true",
  "dependencies": {
    "@popperjs/core": "^2.11.2",
    "bootstrap": "^5.1.3",
    "esbuild": "^0.14.12",
    "sass": "^1.49.0"
  },
  "scripts": {
    "build:css": "sass ./app/assets/stylesheets/application.bootstrap.scss ./app/assets/builds/application.css --no-source-map --load-path=node_modules",
    "build": "esbuild app/assets/esbuild_javascripts/*.* --bundle --sourcemap --outdir=app/assets/builds"
  }
}
